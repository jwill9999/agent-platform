sonar.projectKey=jwill9999_agent-platform
sonar.organization=jwill9999

# Exclude generated artifacts and test fixtures from duplication detection.
# Tests intentionally repeat setup and assertions; they still remain in normal Sonar analysis.
sonar.cpd.exclusions=**/drizzle/meta/*.json,**/dist/**,**/node_modules/**,**/test/**,**/*.test.ts,**/*.spec.ts,**/e2e/**
