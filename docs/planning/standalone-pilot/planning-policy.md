# Proposed scope policy — not an installed runtime policy

Applies only to the proposed .17 repair. Preserve existing runtime security and broker policy.
Changes are limited to workflow-control source/tests and the linked planning, testing, review,
workflow guide, task and session documents. No application behavior, dependencies or external account
configuration changes. No live managed pilot, merge, staging, production or paid runtime calls.

Supervised implementation is the proposed bootstrap mode because the managed implementation path is
what needs repair. This is a proposal requiring execution authorization, not a silent fallback or a
claim that no live run exists. Before mutation, establish exclusive ownership and absence of conflicting
managed ownership through the supported host/operator evidence; unknown ownership blocks execution.

The contract hashes these actual proposal bytes for review identity only. They are not a process
capability, installed runtime policy, grant, or substitute for exact owner approval and persisted bindings.
