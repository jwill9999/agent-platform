# Refine MVP reliability and visibility requirements

## Requirements

Prepare current-stack requirements and a reviewable plan for baseline completion, basic local status,
acknowledged cancellation and interrupted-action reconciliation. Reuse existing tasks and add only
bounded children. Owner authorized planning; the local status panel is an agreed planning requirement.

## Implementation and dependency order

Read live Beads, merged baseline evidence and current paths. Preserve existing broad tasks, blockers
and statuses. Add review-gated child specs, proposed contract and independent critique. No upstream
blocker for this planning work; runtime allocation waits for the separate owner review gate.

## Tests and verification

Validate with the repository execution-contract schema, hash evidence, obtain distinct critic review,
check new Beads edges against specs, validate Markdown/links and confirm planning-only Git diff.
Run required hosted checks on the documentation PR. No runtime experiments or production changes.

## Definition of done

Requirements, non-goals, dependencies, test strategies, allowed paths, branch chain and acceptance
criteria are concrete. Critic findings have dispositions and a clean final review. Files are pushed
and reviewable, Beads synced, owner brief delivered. Segment-tip closure awaits PR merge and hosted
checks; no claim of implementation acceptance. No staging/main promotion.

## Sign-off

Planning authorized September 23. Implementation approval pending. See the
[MVP plan](../planning/mvp-reliability/plan.md).
