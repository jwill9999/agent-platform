# Server-side key for `/api/chat` (same role as API `OPENAI_API_KEY`).
OPENAI_API_KEY=

# Optional: base URL of the HTTP API (for future client-side CRUD). Default in dev: http://localhost:3000
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000
