# Agent Platform — decision log

Single source of truth for **architectural and product decisions** agreed with the project owner.  
**Task execution** is tracked in **bd (beads)**; this file does not replace issue tracking.

---

## Session continuity

- **2026-04-14 (a.m.):** Beads backlog checked against **`main`** (Frontend and Planner epics closed; next executable task **`agent-platform-o36.1`**). No change to **Locked decisions** this review.
- **2026-04-14 (p.m.):** Epic **`agent-platform-o36`** and tasks **`o36.1`**, **`o36.2`** closed in Beads (MVP E2E + operator docs). **Locked decisions** unchanged.
- **2026-04-15 (a.m.):** Full API + harness + runtime assessment completed. Three new epics created in Beads with 13 tasks and detailed specs in `docs/tasks/`. Epics: **`agent-platform-nzq`** (Agent Schema & Factory, 3 tasks), **`agent-platform-n0l`** (Agent Runtime Loop, 6 tasks), **`agent-platform-qlp`** (Harness Hardening, 4 tasks). All work on **`feature/agent-platform-runtime`**. **Locked decisions** unchanged — architecture aligns with the 5-layer harness/runtime model from the project ADR.
- **2026-04-15 (p.m.):** Code review completed — `docs/reviews/2026-04-15-api-harness-review.md`. **8 new Beads issues** created (4 MVP prerequisites: `oss` P0, `pe4` P1, `ptj` P2, `qhe` P2; 4 post-MVP backlog: `fcm` P4, `bto` P3, `nqn` P3, `hnx` P3). Existing tasks `9v1` and `40r` updated with review recommendations. Dependencies wired. Total: **21 issues** (up from 13). **Locked decisions** unchanged.
- **2026-04-15 (evening):** SonarCloud quality gate fixes merged (PR #27). **`agent-platform-pe4`** (replace deprecated `SSEClientTransport` → `StreamableHTTPClientTransport`) completed and merged (PR #28). MCP adapter now uses `StreamableHTTPClientTransport` for both `'sse'` and `'streamable-http'` transport types. 202 tests passing. **`oss`** and **`pe4`** closed in Beads. **Locked decisions** unchanged.
- **2025-07-18 (Harness Security + Docs Audit):** Implemented security guards (`injectionGuard`, `outputGuard`, `mcpTrustGuard`, `totalToolCalls` limit, file size limits, domain allowlist). Full documentation audit: rewrote `docs/configuration.md`, created `docs/architecture/message-flow.md`, updated `docs/api-reference.md` and `docs/architecture.md`, synced all AI instruction files. **Locked decision updated:** `Filesystem / MCP` → `System tools / filesystem` — platform uses built-in system tools with risk-tiered guards (PathJail, bash guard, HITL), not MCP for filesystem access. Branch: `task/harness-security-guards`.
- **Frontend UI:** Unblocked (2026-04-20). Planning doc at **`docs/planning/frontend-ui-phases.md`** still applies as guidance. **`agent-platform-ntf`** promoted to P2, ready for implementation.
- **2026-04-25 (Tier 1 gap remediation epic landed):** Epic `agent-platform-d87` closed via four PRs — `agent-platform-7ga` (critic/evaluator loop), `agent-platform-fc8` (Definition-of-Done contract phase), `agent-platform-2v6` (agent-queryable observability tools), and `agent-platform-n6t` (docs CI + ADR pattern + agent-instructions de-duplication). Architectural record: **[ADR-0001](docs/adr/0001-tier1-gap-remediation.md)**. Shared agent operating rules now live in **`docs/agent-instructions-shared.md`**; `AGENTS.md`, `CLAUDE.md`, and `.github/copilot-instructions.md` are thin wrappers. New CI gate: `.github/workflows/docs-ci.yml` (markdownlint + lychee). **Locked decisions** unchanged.
- **`session.md`** — short narrative of last session, current focus, and next steps (update at end of each agent session when work stops).
- **`AGENTS.md`** — project rules for agents (includes bd workflow).
- **`docs/tasks/README.md`** — task workflow: **each Beads task** links to **`docs/tasks/<issue-id>.md`** (requirements, implementation plan, dependency tables, definition of done, sign-off). **Beads `blocks` edges** are the schedule of record; specs document the same order and capture planning-only dependencies.

---

## Task management: Beads vs Git (single source of truth)

| Concern                                | Source of truth | How to check                                                                                          |
| -------------------------------------- | --------------- | ----------------------------------------------------------------------------------------------------- |
| **What is the next task?**             | **Beads**       | `bd ready --json` (unblocked work)                                                                    |
| **What is open / in progress / done?** | **Beads**       | `bd list`, `bd show <id>`; close with `bd close <id>` when the task spec + Git workflow are satisfied |
| **Dependencies (order)**               | **Beads**       | `bd dep list <id>`; `blocks` edges                                                                    |
| **Commits, branches, merges**          | **Git**         | Normal Git workflow (`feature/…`, `task/…`); **do not** infer Beads status from branch names alone    |

**For you and the agent:** when you say “move on to the next task,” the agent should **`bd ready`** (or **`bd show`** on the agreed next id)—**not** guess from Git alone.

**`.beads/` in this repo:** Beads stores issues in a **local Dolt-backed database** (see `.beads/metadata.json`). The **`dolt/`** directory and **`backup/`** JSONL exports are typically **gitignored**—the **live issue state** is maintained by the **`bd`** CLI and hooks, not by hand-editing JSONL. Tracked files under `.beads/` include **config**, **hooks**, and **`README.md`** (upstream template). Use **`bd`** commands to create/update/close issues; commit code changes in Git as usual. For moving work between machines, follow your Beads version’s sync/backup guidance (`bd` help / project hooks).

**Task spec files (`docs/tasks/<issue-id>.md`):** Leave them **where they are** after a feature or task is done—**no** archiving or deletion for now. If the folder becomes unwieldy later, we can introduce an archiving scheme then; until then, keep the workflow simple.

---

## Thinking stream (MVP recommendation)

**What it is:** A separate channel in the chat stream for **model reasoning** (sometimes exposed by providers as “reasoning tokens”, “thinking”, or extended chain-of-thought). The UI can show it collapsed, behind a toggle, or in a secondary panel so it does not clutter the main answer.

**MVP approach:**

1. Define a stream event type (e.g. `thinking`) in shared contracts alongside `text`, `code`, `tool_result`, `error`.
2. **When the provider supports** exposing reasoning/thinking, map it into that channel.
3. **When it does not**, omit the channel (no fake thinking).
4. Add a **user preference** (e.g. show/hide thinking) in settings when the UI lands.

This keeps one protocol for all providers without hardcoding a single OpenAI model name.

---

## Plugin resolution (MVP)

Effective configuration is resolved in order:

1. **Global plugin catalog** — core plugins (e.g. memory, observability) registered as available.
2. **User-level defaults** — which implementation is active when not overridden.
3. **Per-agent policy** — allowlist/denylist (or explicit plugin list) on a persisted agent so specialists can differ.

Plugins **cannot** bypass harness validation or tool allowlists.

---

## Locked decisions (authoritative)

| Area                          | Decision                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ----------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --- |
| **Users / auth**              | Single user, local-only; no multi-tenant product requirement.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Deployment**                | **Docker** (Dockerfile + **Docker Compose** preferred for MVP). App runs locally; optional extra services (e.g. Postgres) added as separate compose services later.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Backend (`apps/api`)**      | **Node.js** + **TypeScript**. HTTP server: **Express**. **Clean architecture:** separate **domain** and **application** (use cases) from **infrastructure** (DB, MCP, external APIs) and **interface** (HTTP routes/controllers stay thin). Use TypeScript **interfaces/types** and domain models at each boundary; align payloads with **`packages/contracts`** where they cross the API.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| **REST API**                  | **JSON** under **`/v1`** when **`SQLITE_PATH`** is set (skills, tools, mcp-servers, agents, sessions). Request validation with **Zod** schemas from **`packages/contracts`**. Error body **`{ error: { code, message, details? } }`**. **No authentication** for MVP (single-user stub middleware).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| **Database (MVP)**            | **SQLite** on a **Docker volume**. Optional later: **Postgres** (or other) as an expansion path—may be presented as “plug-in” or compose profile when you implement it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| **ORM / migrations**          | **Drizzle ORM** with **better-sqlite3**; SQL migrations live in **`packages/db/drizzle`**; app reads **`SQLITE_PATH`** (see `docker-compose.yml`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Secrets**                   | **AES-256-GCM** envelope (`aes-256-gcm-v1`) in **`packages/db`**; **`SECRETS_MASTER_KEY`** env = **base64 of 32 bytes**; DB holds **`ciphertext_b64`**, **`iv_b64`**, **`auth_tag_b64`**, **`key_version`**, **`algorithm`** on **`secret_refs`** — **never plaintext**. Never log master key, IV, ciphertext, or decrypted values (generic errors only on decrypt failure).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Secret key rotation**       | **`key_version`** on each row; MVP uses version **1** with a single env key. **Rotation:** generate a new 32-byte key, update **`SECRETS_MASTER_KEY`**, bump version in app config for new writes, and **re-encrypt** existing rows with a maintenance script or future UI (not automated in MVP).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| **Model router**              | **OpenAI first** in the UI and router implementation; design the interface so **provider + model id + API key** can be supplied for the **default** agent and for specialists. **No hardcoded single model id**—user-configurable model string for MVP. Resolution chain: **agent `modelOverride`** → **env defaults** (`DEFAULT_MODEL_PROVIDER`, `DEFAULT_MODEL`) → **system fallback** (`openai` / `gpt-4o`). Later: additional providers behind the same abstraction.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| **Agent identity**            | Agents carry a **`systemPrompt`** (required) and **`description`** (optional) alongside governance fields. The system prompt is constructed fresh per request (agent base prompt + skill definitions + tool descriptions) — **not** stored as a message row — so changes to agent config take effect immediately.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| **Runtime execution**         | Two execution modes: **ReAct loop** (default — LLM reasons, calls tools, observes results, repeats until text response or limit) and **plan mode** (LLM generates structured plan, validated against agent allowlists, then executed sequentially). Mode selected per request. Both enforce `ExecutionLimits` (maxSteps, timeoutMs, maxTokens, maxCostUnits).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| **Streaming protocol**        | Chat responses streamed as **NDJSON** (newline-delimited JSON). Each line is an `Output` union event: `text`, `code`, `tool_result`, `thinking`, `error`. Content-Type: `application/x-ndjson`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |     |
| **System tools / filesystem** | **Core default agent** uses **built-in system tools** (`sys_bash`, `sys_read_file`, `sys_write_file`, `sys_list_files` + tiered helpers) with layered security guards: **PathJail** (mount-based path confinement), **bash guard** (command allowlist + blocked patterns), **risk tiers** (zero/low/medium/high), and **HITL approval** for high-risk tools (e.g. `sys_bash`). Running in Docker, the **container filesystem** is the outer boundary—no access to the host OS except what is explicitly **mounted** into the container. MCP servers may provide _additional_ tools but are not the primary filesystem mechanism.                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| **MCP**                       | Standard MCP; users supply **API keys when a server requires them**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| **Tests**                     | Before a task is considered done: **unit tests** and **E2E tests** as **required** for that task’s scope (no blanket boilerplate—each bd issue should state what tests apply). **Minimum before sign-off:** unit tests run and pass for the code touched by the task.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Git / branches**            | **Naming:** **`feature/<feature-name>`** (integration); **`task/<task-name>`** (work units). **No direct commits to `main`.** **Chained tasks (default):** the **first** task in a segment branches from **`feature/<feature-name>`**. Each **next** task branches from the **previous task’s branch** after that task’s work is complete and pushed (linear chain). **Do not** merge each task to `feature` individually—when the **segment** is finished (e.g. Foundation tasks 1–5), open **one PR** from the **tip** of the chain (e.g. **`task/agent-platform-mov.5`**) **→ `feature/<feature-name>`**, bringing in the whole segment. The **first** task of the **next** segment then branches from the **updated** `feature/<feature-name>`. When the overall feature is complete on `feature/<feature-name>`, open **one** PR **`feature/<feature-name>` → `main`**. _Examples:_ `feature/agent-platform-mvp` (Foundation, merged to `main`), `feature/agent-platform-persistence` (Persistence epic), `task/agent-platform-mov.1` … `task/agent-platform-mov.5`. |
| **Token budget**              | Use tokens minimally; if the budget is exhausted, the owner decides whether to increase it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| **Plugin scope (MVP)**        | **Backend lifecycle hooks only** for MVP. Extensibility is covered by: (1) **MCP servers** for external tools, (2) **Skills** for composing tool combinations, (3) **Plugin hooks** for observability/logging. Third-party backend integrations (Slack, Datadog, custom memory) are post-MVP; frontend UI extensions are a separate future concern. The plugin-sdk hook architecture is sound — expansion is additive, not a rewrite.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| **Lazy skill loading**        | Skills are **not** injected in full into the system prompt. Only lightweight **stubs** (name, description, hint) are injected. The model calls `sys_get_skill_detail` on demand to fetch full instructions (goal, constraints, tools). Governor: warn at 3 loads of same skill, error at 5 (loop detection). Schema fields `description` and `hint` are optional—when absent, `goal` is truncated for the stub. This reduces prompt overhead by ~70% for multi-skill agents.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| **Frontend data**             | **API is the single source of truth.** The frontend must never hardcode defaults that the backend owns (model names, token budgets, strategies, limits). When an agent field is `null`/absent, the frontend fetches the platform-level default from `GET /v1/settings` — never embeds a fallback constant. See `docs/planning/frontend-ui-phases.md § Frontend data principles`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |

**Bootstrap (per epic or major line):** create and push **`feature/<feature-name>`** from `main` without changing `main` (e.g. `git checkout -b feature/agent-platform-persistence && git push -u origin feature/agent-platform-persistence`). The **first task** in a segment branches from that **`feature/<feature-name>`**; later tasks branch from the **previous `task/<task-name>`** branch. Delete or stop using an old **`feature/...`** branch after it is merged to `main` if you rotate integration branches per epic.

---

## Definition of Done (default for bd issues)

Unless an issue states otherwise, **done** means:

1. **Acceptance criteria** in the issue are met.
2. **This task’s spec checklist** in `docs/tasks/<issue-id>.md` is **fully checked** (including Git workflow and tests).
3. **Tests:** **Unit tests** run and pass at minimum; **E2E** (or integration) tests when the change crosses API, DB, or Docker boundaries.
4. **Quality:** typecheck passes; linters clean for touched files.
5. **Git:** **Chained segment:** branch from **`feature/<feature-name>`** (first task) or from **previous `task/<task-name>`** (later tasks). **Segment finale:** one **PR** from **tip branch** → **`feature/<feature-name>`** (not `main`). Task branch created **before** implementation. **Overall:** **`feature/<feature-name>` → `main`** only via the agreed release PR.
6. **Documentation:** `decisions.md` updated if a **new** architectural decision was made; `session.md` updated at session end.
7. **Tracking:** issue **closed** in bd with a short reason **after** PR merge and checklist completion; discovered follow-ups filed as new issues.

---

## Pending decisions (return to later)

| Topic                         | Summary                                                                                                                                                                                                         | Status               |
| ----------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| **Plugin authoring guide**    | Plugins can override built-in features (memory, observability, etc). Need a formal guide documenting hook interfaces, override patterns, and how third parties create plugins that interface with the platform. | Deferred to post-MVP |
| **Documentation restructure** | README should be an index/table of contents linking to detailed pages under `docs/`. Each major subsystem (architecture, API, plugins, deployment, configuration) gets its own focused page.                    | Done (1nx merged)    |
| **Observability / metrics**   | Production-grade metrics (Prometheus, Datadog), trace persistence, onMetric hook. Deferred — pino structured logging and trace events are sufficient for single-user local MVP.                                 | Deferred to post-MVP |

---

## References

- `agent_architecture_detailed_adr.md`
- `agent_architecture_full_with_frontend.md`
- `agent_platform_mvp_be346e14.plan.md`
